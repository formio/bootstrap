// import { expect, test } from 'vitest';

// import LabelTemplate from '../form.js';

// test('should correctly render a label given a basic context', function () {
//   const context = {
//     component: {
//       key: 'firstName',
//       type: 'textfield',
//       label: 'First Name',
//       input: true,
//     },
//     iconClass: (icon: string) => `bi-${icon}`,
//     t: (val: string) => val,
//     instance: { id: 'abc123' }
//   };

//   const label = LabelTemplate(context);
//   expect(label).to.equal(
//     `<label>First Name</label>`
//   );
// });
